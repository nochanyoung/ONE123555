# 디자인 스타일

색은 `docs/기획/웹사이트/safe-bet/app/globals.css`의 `@theme` 블록 **한 곳**에만 정의한다.
화면 파일(`app/**/*.tsx`)에 `#2F4156` 같은 색값을 직접 쓰지 않는다. 토큰 이름으로만 쓴다.

**팔레트 출처:** Perky 디자인 토큰
Navy `#2F4156` · Teal `#567C8D` · Sky `#C8D9E6` · Beige `#F5EFEB` · White

**지면은 흰색이다.** Perky는 베이지(`sand-100`)를 지면으로 썼지만 이 앱은 흰 배경을 쓴다.
아래 대비 수치는 전부 흰 배경 기준이라 그대로 유효하다.

---

## 토큰 목록

### ink — 텍스트 (Navy 계열)

| 토큰 | 값 | 용도 | 흰 배경 대비 |
|---|---|---|---|
| `ink-900` | `#2F4156` | 제목, 정책명, 본문 기본색 | 10.44:1 |
| `ink-700` | `#465D79` | 강조 본문, `해당 없음` 태그 글자 | 7.23:1 |
| `ink-600` | `#526A83` | 본문 | 5.60:1 |
| `ink-500` | `#5B6F86` | 보조 설명, 날짜·확인일 메타 | 5.17:1 |
| `ink-200` | `#D5DDE5` | 카드 보더, 입력창 보더 | **텍스트 금지** |
| `ink-100` | `#E8EDF2` | 옅은 면, 고지 문구 배경 | **텍스트 금지** |

### brand — 액션·선택 상태 (Teal / Sky 계열)

| 토큰 | 값 | 용도 |
|---|---|---|
| `brand-900` | `#2C4552` | 선택된 버튼 글자, 금액 강조, 안내 상자 제목 |
| `brand-800` | `#3A5A6B` | 안내 상자 본문 |
| `brand-700` | `#46687A` | 링크, 작은 강조 라벨, hover·눌린 상태 |
| `brand-600` | `#567C8D` | **기본 액션** — CTA 버튼 배경, 선택 보더 |
| `brand-300` | `#A6C2D3` | 점선 보더 |
| `brand-200` | `#C8D9E6` | 선택 보더, 포커스 링 (**텍스트 금지**) |
| `brand-50` | `#EFF4F8` | 선택 배경, 결과 강조 상자 배경 |

### sand — 면 (Beige 계열)

지면이 흰색이 되면서 `sand-100`은 쓰지 않는다.

| 토큰 | 값 | 용도 |
|---|---|---|
| `sand-200` | `#E9DFD8` | 구분선, 진행 바 트랙 |
| `sand-100` | `#F5EFEB` | (미사용 — Perky의 페이지 배경이었음) |
| `sand-50` | `#FAF7F5` | 비활성·`해당 없음` 카드 배경 |

### 상태 — 태그 3종

| 태그 | 배경 / 글자 / 보더 | 대비 |
|---|---|---|
| 🟢 가능성 있음 | `ok-50` `#E4F0EA` / `ok-700` `#2E6A52` / `ok-200` `#B9D6C7` | 5.44:1 |
| 🟡 확인 필요 | `warn-50` `#F8EFDC` / `warn-800` `#7A5518` / `warn-200` `#E8D4AC` | 5.85:1 |
| ⚪ 해당 없음 | `ink-100` / `ink-700` / `ink-200` | 5.75:1 |

---

## 지켜야 할 규칙

1. **`ink-200`, `ink-100`, `brand-200`, `sand-*` 위에 글씨를 올리지 않는다.**
   면과 보더 전용이다. 대비가 안 나온다.

2. **본문 텍스트는 `ink-600` 이상만 쓴다.**
   `ink-500`이 가장 옅게 허용되는 텍스트 색이고, 그보다 옅은 토큰은 없다.
   Tailwind 기본 팔레트의 `text-stone-400`(2.77:1) 같은 색을 되살리지 않는다.

3. **`brand-600`은 배경이나 큰 글씨로만 쓴다.**
   흰 배경 위 대비가 4.50:1로 본문 기준을 겨우 통과한다. 링크나 작은 라벨에는
   `brand-700`(6.44:1)을 쓴다. 버튼 배경으로 쓸 때 그 위 글자는 흰색(4.50:1).

4. **색을 새로 추가하려면 대비부터 확인한다.**
   기준: 본문 4.5:1, 18px 이상 굵은 글씨 3:1 (WCAG AA).
   확인 도구: https://webaim.org/resources/contrastchecker/

---

## 형태 규칙

| 요소 | 규칙 |
|---|---|
| 카드 | `rounded-2xl border border-ink-200 bg-white p-4~5`, 필요시 `shadow-sm` |
| 강조 상자 (결과) | `rounded-2xl border-2 border-brand-600 bg-brand-50 p-5` |
| CTA 버튼 | `rounded-xl bg-brand-600 py-3 text-base font-bold text-white active:scale-[0.99]` |
| 선택 칩 (선택됨) | `border-brand-600 bg-brand-50 text-brand-900` |
| 선택 칩 (기본) | `border-ink-200 text-ink-500` |
| 입력창 | `.input` 유틸 — `border-ink-200`, 포커스 시 `border-brand-600 ring-2 ring-brand-200` |

흰 지면 위에서 카드가 묻히지 않도록 **모든 흰 카드에는 `border-ink-200`을 반드시 붙인다.**

## 폰트

`Pretendard` → `Apple SD Gothic Neo` → `Malgun Gothic` → `sans-serif`

---

## 아직 안 정한 것

**금액 강조용 액센트 색이 없다.** 팔레트 5색이 전부 저채도라 결과 화면의
"최대 지원 가능액"을 띄울 색이 부족해서, 지금은 `brand-50` 배경 위 `brand-900`
글씨로 처리해 뒀다(대비 10.6:1). 대비는 통과하지만 "금액"이라는 신호가 약하다.

후보 방향: 베이지와 붙는 테라코타 계열, 또는 채도를 올린 틸.
정해지면 `app/result/page.tsx`의 금액 두 줄만 바꾸면 된다.
